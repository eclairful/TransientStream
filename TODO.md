### Basically just gonna try to apply separation of concerns:

- [ ] separate css into several different more specific files
- [ ] rename variables & functions to a consistent naming pattern
- [ ] organize functions into smaller & more specific variants
- [ ] organize code into modules, instead of one big file
- [ ] reduce the amount of IDs & classes being used, opting for using css selectors in `querySelector(All)()` instead.

#### Note to self: don't u dare touch the electron app yet lol
